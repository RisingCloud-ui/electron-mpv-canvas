---
name: Bug report
about: Report a problem with the mpv → WebGL bridge
labels: bug
---

**What happened?**

A clear description of the failure (black canvas, garbled frames, crash, ...).

**How to reproduce**

Steps, including the video's codec/resolution if relevant:

1.
2.

**Environment**

- Windows version:
- GPU + driver version:
- Electron version (if using the integration example):
- libmpv SDK source and version (shinchiro / zhongfly / self-built, GPL or LGPL):
- `hwdec-current` from the log, if available:

**Logs**

Console output from the standalone test or the Electron terminal. The
`[worker]` / `[renderer-console]` lines are the useful ones.

**Does the standalone test reproduce it?** (`npm run test:standalone`)

Yes / No / Not tried
